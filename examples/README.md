# Bundled demo input

`demo-playlist.m3u8` is the browser and CLI demo playlist. It has one Unicode-normalization mismatch, one case mismatch, and one missing path.

Run `playlist-path-linter demo` to create the matching temporary sample library and inspect this playlist without modifying your files.
